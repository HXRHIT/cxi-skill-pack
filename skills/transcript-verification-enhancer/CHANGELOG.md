# Changelog — transcript-verification-enhancer

이 파일은 스킬의 **동작(SKILL.md)과 재사용 코드(scripts/)가 실제로 어떻게 바뀌어왔는지**를 세션 단위로 기록한다.

## 기록 규칙

- 새 항목은 파일 맨 위에 추가한다(최신이 위).
- 각 항목: `## YYYY-MM-DD — 한 줄 요약` + `- 계기:` + `- 변경:` + `- 검증:` + `- 남은 일:`
- `scripts/`가 canonical이며, `validation_runs/` 안의 사본은 실행 스냅샷일 뿐이다.

---

## 2026-08-27 — 1차 검증 판정 FAIL, `batch_processor.py` 재작성

- 계기: 사용자 지시로 D 등급 미검증 스킬 순차 검증(claim `V-15`). 2026-08-12에 작성된 뒤 한 번도 실행 검증되지 않은 상태였다.
- **판정: FAIL.** 구버전은 실사용 불가였다.
  - SKILL.md가 지시한 호출 형태로 실행하면 native 전사본 **36건 중 35건에서 즉시 `ValueError`** — `--sheet` 기본값 `"1. 녹취 종합"`이 그 35건에 없다(전부 `Contents`). 유일하게 일치한 1건도 전사본이 아니라 IBUJA 코딩 워크북이었다.
  - 시트명을 고쳐 넘겨도 `IndexError` — `Contents`는 헤더행이 없고 배치가 `[빈칸, 화자, 타임스탬프, 발화]` 4열인데, 구버전은 `columns[2]`=PID, `columns[4]`=Context로 가정했다. 그 전에 화자 병합이 **조용히 no-op**(528→528)이 되면서도 "Merged" 로그를 찍었다.
  - 완주한 1건에서는 워크북 **7시트+차트1 → 1시트+차트0**, 그리고 병합 `'first'` 집계로 `질문` 294셀 · `시간` 291셀 · `#(자동)` 282셀 · `Code` 41셀이 삭제됐다(`Code` 순감 566→531).
- 변경:
  - **`scripts/batch_processor.py` 전면 재작성.** 구버전은 `validation_runs/.../batch_processor_before_fix.py`로 보존.
    - 시트 실측 선택(`Contents` → `1. 녹취 종합` → 단일시트 → 아니면 중단하고 시트 목록 제시). (B1)
    - 컬럼 프로파일링으로 화자·타임스탬프·발화 탐지 — 타임스탬프는 `mm:ss` 비율, 화자는 힌트어(`진행자`/`참석자` 등) 비율 또는 "짧고 고유값 적음", 발화는 평균 길이 최대. `--text-col`로 수동 교정 가능. (B2)
    - **행을 병합하지 않는다.** 인접 발화는 프롬프트 `neighbors` 필드로만 전달하고 결과는 원래 행에 되쓴다. 입력 행 수 = 출력 행 수 불변. (B4)
    - **원본 워크북을 열어 대상 셀만 갱신**하고 저장 → 다른 시트·차트·서식 보존. (B3)
    - 안전 가드 4종: `--out` 필수 / native 경로 쓰기 차단 / 원본 덮어쓰기 차단 / `--i-have-upload-approval` 없이 LLM 호출 거부. (B5·B7)
    - `--inspect`(구조만) · `--dry-run`(LLM 없이 파이프라인 + change log) 추가 — 외부 전송 없이 검증 가능한 경로.
    - `<출력파일>_changelog.md` 자동 생성 — SKILL.md가 약속만 했던 요약표. (B8)
    - **익명화 규칙을 프롬프트에서 제거.** `#14`가 담당하며 그쪽은 치환 전 사람 승인을 요구한다. 구버전 프롬프트는 `P7 -> [P001]` 재치환까지 지시했는데 이는 #14 검증에서 Critical(A1)로 잡힌 항목이다. (B6)
    - 기본 모델을 `gemini/gemini-1.5-flash` → `anthropic/claude-sonnet-5`로 교체. `time.sleep(4)` 고정 대기 제거(재시도 시에만 대기).
  - **SKILL.md** — 3단계 호출 절차(`--inspect` → `--dry-run` → 실제) · native 시트 실측 표 · 외부 전송 게이트 절 · "익명화는 이 스킬이 하지 않는다" 절 · 행/워크북/수식 캐시 보존 원칙 · change log 산출 내역 · `.xls` 미지원 · docx 미지원의 무게(UXQ 8건이 docx이고 화자 라벨이 가장 잘 정리된 케이스) · `_가명화` 접미사 관행.
- 검증 (`validation_runs/transcript-verification-enhancer/2026-08-27_native-transcripts/`):
  - **외부 API를 호출하지 않았다.** 구버전은 `process_chunk`를 identity 스텁으로 교체해(`run_stubbed.py`), 신버전은 `--dry-run`으로 돌렸다. 내부 전사본을 외부로 보내지 않기 위한 조치다.
  - **native 경로를 입력으로 주지 않았다.** 구버전이 출력을 입력 디렉터리에 쓰기 때문에 native에 파일이 생긴다. 대상을 scratchpad로 복사해 실행했다.
  - 재실행 결과: BIZWEB `Contents` 529행 → 시트 자동선택 + 화자1/시간2/발화3 정확 탐지, 행 수 529 유지. IBUJA → **7시트·차트1 보존**, 2550행 유지, `질문`·`시간`·`Code`·`Context` non-null **전부 동일**.
  - 가드 4종 전부 실측으로 중단 확인.
- 남은 일:
  - **정제 품질 미검증** — `--dry-run`으로만 돌렸다. 실제 LLM 호출에는 외부 전송 승인이 필요하다. 승인 후 "오타 교정 정확도 / 뉘앙스 훼손 여부 / 지시대명사 보완 적절성"을 별도 검증해야 한다.
  - **수식 캐시 caveat** — 수식 5,747셀은 보존되지만 계산 캐시값이 사라진다(`data_only` 값 셀 16,542 → 10,811). openpyxl 거동이라 우회 불가. 경고만 넣었다. native `fix_excel_formulas.py` 계열 처리를 연결할지 검토 필요.
  - **docx 경로 미구현** (B10). UXQ 8건이 대상 밖이다.
  - **CSV 경로 미검증** — native 전사본에 csv가 없다.
  - **화자 탐지 휴리스틱의 사각** — 힌트어도 `PID` 이름도 없는 파일을 아직 만나지 못했다.
  - **B5·B6은 사람 결정 대기** — 외부 API 사용 여부, `#14`와의 역할 경계. `리서처_결정대기_목록.md` Q37 참고.
