# Changelog — research-plan-writer-skill

이 파일은 스킬의 **동작(SKILL.md/references)과 재사용 코드(scripts/)가 실제로 어떻게 바뀌어왔는지**를 세션 단위로 기록한다. 목적: AI 에이전트와 대화하며 스킬을 반복 수정할 때, ①무엇을 왜 바꿨는지 ②그 변경이 어느 validation_runs 세션에서 검증됐는지를 다음 세션이 바로 이어받게 하는 것.

## 기록 규칙

- 새 항목은 파일 맨 위에 추가한다(최신이 위).
- 각 항목: `## YYYY-MM-DD — 한 줄 요약` + `- 계기:` + `- 변경:` + `- 검증:` + `- 남은 일:`

### `scripts/`가 없는 이유 (판단 기록, AGENTS.md §4-1)

이 스킬은 **문서 생성·판단형**이며 실행 코드가 없다. 입력이 사람의 기획 맥락(비즈니스 질문·제약·일정)이고 출력이 서술형 문서라, 결정적으로 재현할 계산이 없다. 확인을 안 해서 코드가 없는 게 아니라 **원래 필요하지 않다**는 판단이다.

단, 예외 후보 1건이 있다: RQ/RH 마스터가 스프레드시트로 운용되므로(아래 2026-08-27 항목 P3·P4), 향후 `rq-list`/`rh-list` 스켈레톤 xlsx를 생성하거나 RH × 방법론 매트릭스의 공백(어떤 방법론도 검증하지 않는 RH)을 검사하는 코드는 `scripts/`에 둘 가치가 있다. 지금은 미착수.

---

## 2026-08-27 — 1차 검증(24.S.BIZWEB · 25.C.FACE_CORE 홀드아웃) 결과 Critical/High 5건 반영

- 계기: 사용자 지시로 "D 등급(SKILL.md 작성 완료) 중 미검증 스킬을 차례대로 검증" 착수. 이 스킬이 첫 대상. SKILL.md는 26.GP.UXQ·25.S.MTSUX·25.S.1QAI 3건에서 뽑은 7블록 구조 기반이었으므로, 그 3건에 없는 프로젝트로 홀드아웃 검증을 수행했다.
- 변경:
  - **SKILL.md**
    - `### 2a` 신설 — RQ를 3~5개 평면 목록이 아니라 계층(top-level → `ㄴ` → `ㄴㄴ`)으로 쓰게 하고, 광범위 진단형에서는 상위 RQ가 10개 이상일 수 있음을 명시. 계획서 본문엔 상위 RQ만, 전체 계층은 별도 RQ 마스터로 분리. (검증 결함 P3)
    - `### 2b` 신설 — RQ가 seed 목록(생각리스트)에서 triage로 승격된다는 전단계를 Inputs 흐름에 추가. seed의 작성자·출처를 RQ에 승계. (P7 부분 반영)
    - `### 2` RH 규칙 교체 — `상위 RH`(일반 명제) → `세부 RH`(대상 단위 인스턴스화) 2층 구조, 그리고 매핑을 **RH × 방법론 매트릭스**(RH가 주키)로 규정. 기존의 `방법론 블록 → supports: RH1, RH3` 단방향 표기가 실제 팀 구조와 방향이 반대였다. (P4)
    - `### 3` — Stage 1 → Stage 2 항목 승계 표 추가. Stage 1 전용 4항목(방법론 선택 근거·RH·성공 지표·가정/미결정/의존성)이 7블록에 대응 칸이 없어 Stage 2에서 유실되는 문제를 막는다. (P11)
    - `### 5` tone 규칙 교체 — 격식체(`~함/~임`) 단일 규정을 **섹션 성격별 분기**(내부/기록 = 격식체, 제안/대외 = 경어체)로 바꿨다. 25.C.FACE_CORE 계획서 한 파일 안에서 두 문체가 공존하는 것을 실측했다. (P8)
  - **references/plan-patterns.md**
    - `## 1a` 신설 — 선택 블록 2종(`연구동향`, `성과목표(정량/정성)`) 추가. 성과목표는 기대 산출물과 다른 블록임을 명시. 연구동향의 소스로 `$ux-benchmarking-skill` 연결. (P9)
    - `## 1b` 신설 — 문서 층위 구분(과제 단위 vs 차수 단위). 반복 단위가 방법론인지 연구 내용 영역인지가 층위에 따라 달라진다. (P10 부분 반영)
    - `## 2` — 방법론 블록의 `supports:` 라인은 편의 표기이고 정본은 RH × 방법론 매트릭스임을 명시. (P4)
    - `## 3` — RQ 계층/RH 2층 구조 요약 + **IF/THEN 프레임을 요구하지 말라**는 절 신설. 24.S.BIZWEB RH 마스터의 IF/THEN·독립/종속변수 컬럼이 163행 전부 공란인 것을 실측한 근거. (P3·P4·P5)
    - `## 4` tone — 격식체/경어체 예문 병기. (P8)
    - `## 6` — Stage 2가 Stage 1보다 얇아지지 않게 하는 문장 추가. (P11)
    - `## 7` failure modes 4개 추가(계층 평면화, RH 인스턴스화 누락, Stage 항목 유실, 제안 섹션에 격식체 강제).
- 검증: `validation_runs/research-plan-writer-skill/2026-08-27_24.S.BIZWEB/` — `00_source_snapshot.md`(홀드아웃 실측), `01_stage1_brief.md`·`02_stage2_full_plan.md`(스킬 산출물), `03_holdout_diff.md`(결함 P1~P11), `04_validation_notes.md`(판정 CONDITIONAL PASS). 위 변경은 **결함 발견 후 반영한 것이고, 반영한 규칙으로 재생성해 다시 대조하지는 않았다.**
- 남은 일:
  - **재생성 라운드**: 수정된 SKILL.md로 같은 홀드아웃을 다시 생성해 P3·P4·P8·P11이 실제로 해소되는지 확인.
  - **blind 라운드**: 계획서를 열지 않은 프로젝트에 먼저 생성 → 이후 대조. 이번 검증은 실물을 먼저 읽은 뒤 생성했으므로 "팀과 같은 RQ를 도출하는가"는 미검증.
  - Medium 결함 5건(P1 블록 순서 · P2 5슬롯 과생성 · P6 RQ 차수 추적 컬럼 · P7 triage 규칙 세부 · P10 층위 판별 기준)은 리서처 결정 대기 — `리서처_결정대기_목록.md` 참고.
  - Stage 1 정지 규칙 미검증(이번엔 의도적으로 통과시켰음).
  - 출력 포맷 규칙 부재: 실제 팀 계획서는 docx / pptx / xlsx로 갈리는데 SKILL.md는 "default to markdown"뿐이다.
