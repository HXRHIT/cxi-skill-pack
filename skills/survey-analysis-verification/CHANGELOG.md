# Changelog — survey-analysis-verification

이 파일은 스킬의 **동작(SKILL.md/references)과 재사용 코드(scripts/)가 실제로 어떻게 바뀌어왔는지**를 세션 단위로 기록한다.

## 기록 규칙

- 새 항목은 파일 맨 위에 추가한다(최신이 위).
- 각 항목: `## YYYY-MM-DD — 한 줄 요약` + `- 계기:` + `- 변경:` + `- 검증:` + `- 남은 일:`
- `scripts/` 아래 코드가 바뀌면 반드시 이 파일에도 기록한다. **`scripts/`가 canonical**이며, validation_runs 안의 프로젝트별 사본은 실행 스냅샷일 뿐이다.

---

## 2026-08-19 (2차) — 실제 canonical 스크립트 작성 및 검증: 이 스킬도 코드가 필요한 게 맞았음

- 계기: 사용자가 "#17/#18/#39도 실제로 코드 저장해서 검증해줘" 요청. 직전 항목에서 "설계상 코드가 필요 없을 수도 있다"고 남겨뒀던 질문에 대한 답: **아니다, 코드가 필요하다.** 이 스킬은 실제 상관계수·평균비교를 계산하는 스킬이라 체크리스트 대조가 아니었다.
- 변경: `scripts/verify_findings.py` 신규 작성. RQ/RH↔문항 매핑(연구자 판단)은 `rh_config.json`(데이터)으로 분리하고, 통계 계산(상관계수, count-to-outcome)은 코드로 처리하는 구조로 설계 — 매핑은 프로젝트마다 바뀌지만 계산 로직은 재사용됨.
- 검증: `validation_runs/survey-analysis-verification/2026-08-19_26.GP.UXQ/04_validation_notes.md` — RH02(r=0.702) 및 RH05(r=0.154, mean_by_count 전부)가 2026-08-17 원본과 정확히 일치. 과정에서 두 가지 실수를 고침: ①"불편함을 경험한 적 없음" 옵션을 friction count에 잘못 포함시켰던 것, ②(basic-stats와 동일 교훈) sample vs population 표준편차.
- 남은 일: RH03(기존 대비 불편 항목 정의 문항 미확인)과 RH09(P20B1의 "긍정 이유" 값 미확정)는 이번 세션에서 재구성하지 못함 — `rh_config.json`에 추가하기 전 해당 문항을 다시 찾아야 함.

- 계기: 사용자가 "나머지 데이터분석/대시보드 스킬에도 소급 적용해줘" 요청 → scripts/ 승격 시도 중 코드가 없음을 발견해 보고했더니, 사용자가 "그럼 #17/#18/#39 검증 자체를 한 게 맞냐"고 정당하게 재질문함.
- **재확인 결과 (중요, 오해 방지)**: `uxq_survey2/verified_key_findings.json`을 직접 열어보니, RQ/RH ID(`26.GP.UXQ-R04-RQ02` 등 실제 RQ 카탈로그 체계와 일치)에 연결된 상관계수·평균 비교 5건이 `verification_status`(verified / verified_descriptive)와 `caveat`(예: "No inferential test", "Correlation only, no p-value")까지 구체적으로 명시돼 있음 — 플레이스홀더가 아니라 실제 분석 결과다. **검증은 real이고, 오히려 이 라인 중 가장 정교한 산출물이다.**
- **없는 것은 "코드"이지 "검증"이 아니다**: 다만 이 스킬은 원래 "가벼운 입력 문서 + AI 판단"을 지향하는 철학(#25와 유사)이라, 체크리스트 대조·판정 자체가 애초에 코드화 대상이 아니었을 가능성이 있다 — 이는 #17/#18과 달리 "코드를 안 남긴 실수"가 아니라 "설계상 코드가 필요 없는 스킬"일 수 있다는 뜻이라 다르게 봐야 한다.
- 변경: 없음(승격할 코드가 없어 `scripts/`를 만들지 않았다).
- 남은 일: 이 스킬이 정말 "코드 없이 체크리스트 대조"로 설계된 것인지, 아니면 #17/#18처럼 단순히 코드를 안 남긴 것인지는 다음 세션에서 명확히 판정해 이 CHANGELOG에 기록할 것.
